# Backend Configuration
# MongoDB Atlas connection string (REQUIRED in production)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/agenda?retryWrites=true&w=majority

# JWT Secret for token signing (REQUIRED)
# Generate a strong random string for production
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port (default: 5000)
PORT=5000

# Environment (development or production)
NODE_ENV=development

# Frontend URL for CORS (REQUIRED in production for Netlify)
# Set this to your Netlify deployment URL, e.g., https://your-app.netlify.app
FRONTEND_URL=https://your-app.netlify.app

# Frontend Configuration
# API URL for frontend to connect to backend
# Development: http://localhost:5000/api
# Production: Set to your backend API URL (e.g., https://your-backend.herokuapp.com/api or your server URL)
NEXT_PUBLIC_API_URL=http://localhost:5000/api
